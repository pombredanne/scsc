#!upstart
description "proxymachine socks4a resolver for tor"

start on startup
stop on shutdown

respawn

setuid nobody
setgid nogroup

script
  proxymachine -p <%= node["tor"]["socks-resolver-port"] %> -c /opt/tor-dns-proxy.rb
end script
